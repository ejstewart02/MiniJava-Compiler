class Factorial{
    public static void main(String[] a){
	    System.out.println(10);
    }
}

class A {
    int z;
    public int method1A() {
        int a;

        return a;
    }
}

class B extends A {
    int z;
    public int method1A(int q, int p) {
        int a;

        a = 1;

        return a;
    }
}

class C {
    public int method1C() {
        int a;

        B sym;
        sym = new B(); //make sure sym is of type B

        a = sym.method1A(a, a); //DONE need to ensure # of arguments match and that the arguments of right type

        return a; //Need to make sure a is initialized before use
    }
}
